<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cy.mapper.PlanMapper">
	<select id="GetPlanDetail" resultType="PlanParam">
		SELECT p.post_name,p.post_img,d.pd_img,t.teacher_img,t.teacher_name,pr.price FROM tbl_post p 
		JOIN tbl_postdetail d ON p.pd_id = d.pd_id 
		JOIN tbl_teacher t ON d.teacher_id = t.teacher_id 
		JOIN tbl_price pr ON t.teacher_id = pr.teacher_id
		WHERE p.post_id = #{post_id}
	</select>
	
	<select id="GetCast" resultType="CastParam">
		select p.post_name,p.post_img,c.cast_img,c.cast_text FROM tbl_cast c JOIN tbl_post p
		ON c.post_id = p.post_id
		WHERE p.post_id = #{post_id}
	</select>

</mapper>